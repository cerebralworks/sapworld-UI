export interface tabInfo {
  tabNumber: number;
  tabName: string
}

export interface tabProgressor {
  currentTab: number;
  currentTabName: string;
  nextTab: number;
  nextTabName: string;
  btnType: string;
}
